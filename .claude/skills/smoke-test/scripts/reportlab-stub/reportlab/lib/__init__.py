class _Colors:
    """Colors are only ever passed back into stub TableStyle commands, so
    they just need to be distinct, printable values."""

    black = "black"
    white = "white"


colors = _Colors()
