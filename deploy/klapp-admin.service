[Unit]
Description=Klaus Field Log (admin site)
After=network.target

[Service]
Type=simple
User=tklaus
WorkingDirectory=/opt/klapp
ExecStart=/opt/klapp/admin -addr=:8082
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
